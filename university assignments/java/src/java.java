import java.util.Scanner;
public class java {
    public static void main(String[] args) {
        Scanner scan=new Scanner(System.in);

        Circle circle;
        Point point;
        Line line;
        Rect rectangle;
        Size size;

        // We can initialize classes later

        /*
        EX:
        circle=new Circle(new Point(scan.nextDouble() ,scan.nextDouble()), new Line(new Point(...), new Point(...))
         */

    }
}

